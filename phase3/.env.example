# Docker Hub Configuration (REQUIRED - Must be configured during setup)
DOCKER_USERNAME=
IMAGE_TAG=

# Database Configuration (Optional - Default values provided)
DB_USER=postgres
DB_PASSWORD=mysecretpassword
DB_NAME=notes_app_db
